import VaultView from '@/components/dashboard/VaultView';

export const metadata = {
  title: 'Signal Sync Vault | Coppr',
  description: 'Manage your automated trading synchronization and broker connections.',
};

export default function VaultPage() {
  return <VaultView />;
}
